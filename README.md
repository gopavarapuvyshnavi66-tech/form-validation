# form-validation
A responsive registration form built using HTML, CSS, and JavaScript that performs client-side validation for name, email, and password fields using regular expressions and DOM manipulation to provide real-time error feedback.
